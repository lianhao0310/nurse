# 虚拟环境（体积大，不入库；README 已说明用内置 .venv 或自行安装）
.venv/
venv/
env/

# Node / Capacitor 依赖（体积大，不入库；CI 用 npm ci 重建）
node_modules/
.capacitor/

# Python 缓存
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/

# 本地配置 / 密钥
.env
*.local

# 系统 / 编辑器
.DS_Store
.idea/
.vscode/

# 同步调试 / 一次性脚本（不入库）
.venv_secret/
.set_secret.py
_gh_sync_raw.yml
_run_logs*.zip
_run_*.zip

# 本地 GitHub PAT（仅供本机下载脚本使用，切勿入库）
.ipa_config

# 构建产物下载目录
builds/
