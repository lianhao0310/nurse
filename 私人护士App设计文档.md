# 私人护士 App 产品与技术设计文档（v1.0）

> 文档类型：产品设计 + 技术设计（前期规划，暂不进入编码开发）
> 版本：v1.0
> 创建日期：2026-08-12
> 状态：设计评审稿
> 配套引擎：[私人护士：多病种医嘱解析引擎（v1.0）]

---

## 0. 文档说明

本文档为「私人护士」App 的**前期完整设计**，覆盖产品定位、用户场景、功能架构、核心算法引擎、系统架构、数据模型、提醒调度、隐私合规与技术选型。本阶段目标是以设计文档形式对齐方案，**不启动正式开发**，但所有模块需具备可落地性。

设计的核心能力继承自《私人护士：多病种医嘱解析引擎（v1.0）》中定义的四模块解析能力，并将其工程化为一个可运行的移动端产品。

---

## 1. 背景与目标

### 1.1 痛点
- **听不懂**：老年患者面对医生口头医嘱，常因方言、语速、专业术语而听不懂、记不全。
- **记不住**：一次就诊可能涉及 4~6 种药物、多种剂量频次与复诊安排，纸质小条易遗失。
- **做不对**：用药时间（空腹/饭后）、饮食禁忌（高血压忌腌制、糖尿病忌高糖）执行偏差大。
- **没人管**：独居老人、慢病患者缺乏持续照护，子女远程无法掌握执行情况。
- **不会急**：出现低血压、低血糖等异常时，不知道何时该就医。

### 1.2 产品定位
一句话：**把一次医患对话，自动变成患者（及子女）看得懂、能执行的私人护理计划。**

私人护士是一款面向**慢病患者与老年人群**的 AI 护理助手 App，以「录音 → 解析 → 提醒 → 预警 → 协同」为主链路，将非结构化的问诊对话转化为结构化的护理执行体系。

### 1.3 设计目标
| 维度 | 目标 |
|------|------|
| 准确率 | 药物/剂量/频次关键信息抽取准确率 ≥ 95%（人工可校验） |
| 上手成本 | 老人 3 步内完成一次录音（适老化） |
| 时效性 | 录音结束 60 秒内生成完整护理计划 |
| 可干预 | 所有 AI 生成内容支持人工编辑、纠错、确认 |
| 可信赖 | 不替代医生诊断，仅做「执行层」转化与提醒 |

---

## 2. 目标用户与场景

### 2.1 用户画像
| 角色 | 特征 | 核心诉求 |
|------|------|----------|
| 主用户（患者） | 55+ 慢病患者，高血压/糖尿病/冠心病等多见 | 听得懂、记得住、有人提醒 |
| 照护者（子女） | 30~50 岁，异地或同住 | 远程掌握父母用药与健康 |
| （扩展）家政/护工 | 上门照护 | 按清单执行、上报异常 |

### 2.2 核心场景
1. **门诊就诊**：诊室内用 App 录音，离院即可看到生成的用药与待办。
2. **复诊随访**：按引擎计算的复诊日期提前推送，带齐资料。
3. **居家执行**：到点用药提醒、测血压/血糖提醒、步行任务提醒。
4. **异常处置**：出现头晕/低血糖等症状，App 给出风险预警与应对建议。
5. **家属协同**：子女端同步查看父母护理计划与执行记录，可留言提醒。

---

## 3. 产品功能架构

```
私人护士 App
├── 首页（今日护理看板）
│   ├── 待办时间轴（用药/监测/复诊/生活）
│   ├── 一键「开始录音」入口
│   └── 异常预警横幅
├── 录音与解析
│   ├── 实时录音 / 导入音频
│   ├── 语音转写（ASR，医生-患者角色分离）
│   └── 调用医嘱解析引擎 → 生成四模块
├── 我的用药
│   ├── 药物清单（标准化表格）
│   ├── 用药提醒管理（开关/改时/停用）
│   └── 用药打卡记录
├── 护理任务
│   ├── 监测项（血压/血糖…）
│   ├── 复诊提醒（倒计时）
│   └── 生活任务（步行/休息）
├── 护士叮嘱
│   ├── 饮食建议 / 禁忌清单（按病种）
│   └── 风险预警知识卡
├── 健康档案
│   ├── 病程时间线（每次问诊记录）
│   ├── 指标趋势图（血压/血糖）
│   └── 资料库（检查单/处方照片）
└── 我的 / 家属协同
    ├── 家属绑定与权限
    ├── 数据共享开关
    └── 适老化设置（大字号/语音播报）
```

---

## 4. 核心功能详解

### 4.1 录音采集与转写
- **入口**：首页常驻大按钮「开始录音」，支持后台录音、息屏续录。
- **转写**：调用端侧或云端 ASR，输出带时间戳的文本；通过声纹/话者分离区分「医生」与「患者」，标注发言角色，提升解析准确度。
- **隐私**：录音文件默认本地加密存储，转写后可选择删除原始音频；上传云端仅用于解析，解析后立即清除缓存。
- **兜底**：支持导入微信收到的语音/音频文件，离线录音后导入解析。

### 4.2 医嘱解析引擎（产品核心）
将转写文本送入《多病种医嘱解析引擎（v1.0）》，输出以下四模块（详见第 5 章）：
1. 药物清单（标准化表格）
2. 行为代办清单（任务流）
3. 私人护士叮嘱（饮食与禁忌）
4. 风险预警（异常处理）

> 引擎只做「执行层转化」，不对疾病下诊断、不推荐新药，输出显著标注「仅供参考，请遵医嘱」。

### 4.3 用药提醒
- 由药物清单的「单次剂量 / 每日频次 / 具体服用时间」自动生成多组定时提醒。
- 提醒内容含：药名、剂量、饭前/饭后、特别提醒（如「加量/减量/停药」高亮）。
- 支持：到点推送 + 语音播报 + 用药打卡（已服/跳过/补服）。
- 连续未打卡触发家属端通知。

### 4.4 护理任务流
- **监测项**：如「早晚测血压」「监测餐后血糖」，生成测量提醒与数值录入；录入值自动绘制趋势曲线。
- **复诊**：引擎精准计算下次复诊日期，给出 N 天倒计时与「就诊前准备清单」。
- **生活任务**：如「每日步行 30 分钟」「静坐休息」，生成轻量打卡。

### 4.5 私人护士叮嘱
- 根据对话识别的病种（高血压/糖尿病等），自动匹配权威中西医禁忌与饮食建议。
- 以「可做 / 忌做 / 推荐食材」卡片化呈现，支持语音朗读。
- 示例：高血压 → 忌食腌制食品、避免剧烈起身；糖尿病 → 控糖、定时定量。

### 4.6 风险预警与异常处置
- 基于对话提及及医学常识建立「异常场景 → 预警」规则。
- 患者上报症状（或监测值越界）时，弹出分级预警：
  - 绿色：居家观察 + 自我处理建议
  - 黄色：加强监测 / 联系家属
  - 红色：立即就医指引（含就近医院、急诊提示）
- 所有预警记录进档案，供复诊时回顾。

### 4.7 健康档案与病程
- 每次问诊生成一条「病程记录」（录音/转写/四模块结果/编辑痕迹）。
- 指标（血压、血糖等）自动汇总趋势图，支持导出给医生。
- 资料库：处方、检查单照片 OCR 归档。

### 4.8 家属协同
- 患者授权绑定家属（微信/手机号），家属端查看护理计划与执行率。
- 家属可留言提醒、接收异常通知、代为修改部分设置（需患者授权范围）。
- 隐私边界：病历级内容默认仅患者可见，可显式共享。

---

## 5. 医嘱解析引擎详细设计

### 5.1 角色设定（继承 v1.0）
> 你是一位拥有 20 年经验的高级「私人护理专家」，擅长从混乱的医患对话中精准提取核心医嘱，并将其转化为患者（及子女）看得懂、能执行的标准化护理计划。

### 5.2 输入
- 【原始问诊录音】转写文本（含发言角色标注）。
- 可选上下文：患者已建档病种、既往用药、过敏史。

### 5.3 输出四模块规范

**模块一：药物清单（标准化表格）**
字段：药品全称（纠正同音字/错别字）、单次剂量、每日频次（如 1 次/日）、具体服用时间（如 饭后 20 分钟）、针对病种。
逻辑检查：若提及「加量/减量/停药」须在备注高亮标注。

**模块二：行为代办清单（任务流）**
- 监测项：如「早晚测血压」「监测餐后血糖」
- 复诊：精准计算下次复诊大致日期
- 生活项：如「每日步行 30 分钟」「静坐休息」
- 提取所有非药物指令

**模块三：私人护士叮嘱（饮食与禁忌）**
- 根据病种自动匹配权威中西医禁忌与饮食建议
- 禁忌（如忌食腌制食品、避免剧烈起身）+ 推荐食材 + 风险预警

**模块四：风险预警（异常处理）**
- 针对对话提及或医学常识可能遇到的异常情况，给出预警提示与分级应对

### 5.4 解析管线（工程化）
```
录音音频 → ASR 转写（角色分离）
  → 文本清洗（去口头禅/分段/标点）
  → 病种初判（NLU 关键词 + 引擎识别）
  → 调用 LLM 解析引擎（四模块 Prompt）
  → 结构化校验（剂量/频次合法性、字段完整性）
  → 生成可编辑护理计划（落库 + 触发提醒）
```

### 5.5 病种知识库（支撑模块三/四）
- 内置常见病种（高血压、2 型糖尿病、冠心病、高血脂、慢阻肺等）的：
  - 饮食宜忌、生活禁忌、监测重点、常见异常与应对。
- 知识来源标注权威出处（指南/药典），可迭代更新，避免模型臆造。

### 5.6 质量与可信
- **必带免责声明**：「本结果由 AI 根据录音生成，仅供参考，具体以医生处方为准。」
- **人工校验闭环**：生成后患者可逐条确认/编辑，编辑结果回流用于效果评估。
- **不清算冲突**：若与既往用药冲突（如重复成分），弹窗提示人工核对。

---

## 6. 系统总体架构

### 6.1 端云分层
```
┌─────────────── 移动端（iOS / Android） ───────────────┐
│  录音采集 · ASR（端侧可选）· UI 看板 · 提醒调度 · 本地加密库 │
└───────────────────────┬──────────────────────────────┘
                         │ HTTPS（TLS）
┌───────────────────────▼──────────────────────────────┐
│  云端服务                                               │
│  网关 · 账户/权限 · 音频转写服务 · 解析引擎服务(LLM)      │
│  · 病种知识库 · 结构化存储 · 推送服务 · 家属协同服务       │
└───────────────────────────────────────────────────────┘
```

### 6.2 关键服务
| 服务 | 职责 |
|------|------|
| 转写服务 | 音频 → 带角色文本 |
| 解析引擎服务 | 封装 v1.0 引擎 Prompt，输出四模块 JSON |
| 计划编排服务 | 将四模块转为提醒/任务/叮嘱实体 |
| 调度与推送服务 | 定时提醒、异常通知、家属通知 |
| 档案服务 | 病程、指标、资料存储与检索 |
| 协同服务 | 家属绑定、共享权限、消息 |

### 6.3 数据流转时序（以一次就诊为例）
1. 患者点「开始录音」→ 端侧采集音频。
2. 录音结束 → 上传音频/文本至云端。
3. 转写服务产出角色化文本。
4. 解析引擎服务调用 LLM → 四模块结构化结果。
5. 计划编排服务生成用药提醒 + 任务流 + 叮嘱卡。
6. 结果回传端侧看板，同时推送家属端（若已绑定）。
7. 后续按调度触发用药/监测提醒，异常时触发预警。

---

## 7. 数据模型设计（核心实体）

### 7.1 实体关系
```
Patient(患者) 1──* Consultation(问诊记录)
Consultation 1──1 Transcript(转写)
Consultation 1──* Medication(药物)
Consultation 1──* Task(任务)
Consultation 1──* Advice(叮嘱)
Consultation 1──* RiskRule(风险规则)
Patient 1──* Reminder(提醒)  ← 由 Medication/Task 派生
Patient 1──* Measurement(监测值)
Patient *──* Caregiver(家属, 授权)
```

### 7.2 核心表（关键字段示意）
- **Medication**：id, consult_id, name_std, dose, freq, time_desc, disease, note(高亮加量/减量/停药), status
- **Reminder**：id, source_type(medication/task), source_id, fire_time, repeat_rule, done, skip_reason
- **Task**：id, consult_id, type(monitor/revisit/life), title, detail, due_date, freq
- **Advice**：id, consult_id, disease, taboo[], diet[], risk_note
- **RiskRule**：id, disease, trigger(症状/阈值), level(green/yellow/red), action
- **Consultation**：id, patient_id, audio_ref, transcript_ref, engine_ver, created_at
- **Measurement**：id, patient_id, type(bp/bs), value, measured_at
- **CaregiverBinding**：id, patient_id, caregiver_id, scope, status

---

## 8. 提醒调度与推送

### 8.1 提醒生成规则
- 用药提醒：由 `freq` + `time_desc` 展开为具体时间点（如「饭后 20 分钟」绑定到用餐时间 ± 偏移）。
- 监测提醒：按监测项频次（早/晚）生成。
- 复诊提醒：基于 `due_date` 生成 T-7 / T-1 / 当天三级提醒。

### 8.2 调度策略
- 端侧本地闹钟为主（息屏可靠触发），云端推送为辅（防漏）。
- 支持「用药窗口」柔性提醒（提前 10 分钟 + 到点 + 延后 30 分钟）。

### 8.3 交互
- 推送点击直达对应打卡页；语音播报药名与剂量；大按钮「已服用 / 稍后 / 跳过」。

---

## 9. 隐私、安全与合规

- **数据分级**：原始音频（高敏）、转写文本（高敏）、健康档案（敏感个人信息）分级管理。
- **加密**：传输 TLS；存储端侧 AES 加密；云端字段级加密 + 脱敏。
- **最小化**：原始音频默认不长期留存，解析后清理；用户可自主删除。
- **合规**：遵循《个人信息保护法》《数据安全法》；医疗健康信息按敏感个人信息处理，获取单独同意；不用于训练公开模型（除非用户授权）。
- **免责**：全程显著标注 AI 辅助、非诊疗建议。

---

## 10. 技术选型（推荐）

| 层 | 选型 | 说明 |
|----|------|------|
| 移动端 | Flutter / React Native（或原生 iOS+Android） | 跨端一致、适老化组件易统一 |
| 本地存储 | SQLite + 端侧加密 | 离线可用 |
| ASR | 云端 ASR（带角色分离）+ 端侧热词 | 医药专有名词热词提升准确率 |
| 解析引擎 | 大模型 API（封装 v1.0 Prompt） | 结构化 JSON 输出 + 校验 |
| 云端 | 微服务（容器化） + 对象存储（音频） | 弹性、可审计 |
| 推送 | 厂商通道 + 端侧闹钟 | 高到达 |
| 知识库 | 结构化病种库 + RAG 检索 | 支撑叮嘱/预警权威性 |

---

## 11. 关键交互流程（时序）

**就诊 → 护理计划生成**
```
患者App ──录音──▶ 云端ASR ──文本──▶ 解析引擎(LLM)
   │                                        │
   │◀────── 四模块结构化结果 ───────────────┘
   │                                        │
   ├─ 看板展示 + 人工校验 ──▶ 计划编排 ──▶ 生成提醒/任务
   └─ 推送家属端（若绑定）
```

**异常处置**
```
患者上报症状/监测越界 ──▶ 风险规则匹配 ──▶ 分级预警卡
        │                                        │
        └── 红色：就医指引 + 通知家属 ◀──────────┘
```

---

## 12. 非功能性需求
- **性能**：解析端到端 < 60s；看板首屏 < 1s（本地优先）。
- **可用性**：离线可查看历史计划与打卡；联网同步。
- **适老化**：大字号、高对比、语音播报、极少步骤、防误触。
- **可观测**：解析成功率、提醒到达率、用药依从率指标看板。

---

## 13. 适老化设计要点
- 首页仅保留「录音」与「今日待办」两大区块。
- 所有文本支持语音朗读；提醒用语音而非纯文字。
- 用药打卡用「大圆按钮」，避免复杂表单。
- 家属可远程代为配置，降低老人操作负担。

---

## 14. 迭代路线（MVP → v3）

| 阶段 | 范围 | 目标 |
|------|------|------|
| **MVP** | 录音转写 + 四模块解析 + 用药提醒 + 单病种(高血压/糖尿病) | 验证「录音→计划」主链路 |
| **v2** | 多病种知识库、监测趋势、复诊管理、家属协同、OCR 资料 | 形成完整护理闭环 |
| **v3** | 异常预警分级、可穿戴数据接入、用药冲突检测、医生端共享 | 主动式健康管理 |

---

## 15. 风险与应对
| 风险 | 应对 |
|------|------|
| ASR 医药专有名词识别差 | 医药热词库 + 同音纠错（引擎已含） |
| 解析遗漏/误读 | 人工校验闭环 + 免责声明 + 冲突检测 |
| 医疗合规 | 敏感信息单独同意、不诊疗、可删除 |
| 老人不愿用 | 家属代管 + 适老化 + 语音交互 |
| 模型幻觉 | 知识库约束 + 字段校验 + 出处标注 |

---

## 附录 A：解析引擎 Prompt 模板（沿用 v1.0）
```
你是拥有 20 年经验的高级私人护理专家，擅长从混乱的医患对话中
精准提取核心医嘱，转化为患者（及子女）看得懂、能执行的标准化护理计划。

请对提供的【原始问诊录音（转写文本）】进行深度解析，输出以下四模块：
1. 药物清单（标准化表格）：药品全称（纠正同音字错别字）、单次剂量、
   每日频次、具体服用时间、针对病种；提及加量/减量/停药的在备注高亮。
2. 行为代办清单（任务流）：监测项、复诊（计算下次日期）、生活项；
   提取所有非药物指令。
3. 私人护士叮嘱（饮食与禁忌）：按病种匹配权威中西医禁忌与饮食建议。
4. 风险预警（异常处理）：基于对话或医学常识给出预警与应对。

输出为结构化 JSON，并附「仅供参考，请遵医嘱」声明。
```

## 附录 B：输出示例（药物清单片段）
| 药品全称 | 单次剂量 | 频次 | 服用时间 | 病种 | 备注 |
|----------|----------|------|----------|------|------|
| 苯磺酸氨氯地平片 | 5mg | 1 次/日 | 晨起空腹 | 高血压 | — |
| 二甲双胍缓释片 | 0.5g | 2 次/日 | 早/晚饭后 20 分钟 | 糖尿病 | 一周后**加量至 1g** |

---

*本文档为前期设计稿，配套《私人护士：多病种医嘱解析引擎（v1.0）》共同构成产品与算法基线。*
